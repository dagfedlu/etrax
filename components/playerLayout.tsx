"use client"

import { Box } from "@chakra-ui/layout"

const PlayerLayout = ({ children }) => {
  return (
    <Box>
      Layout - common - here
      {children}
    </Box>
  )
}

export default PlayerLayout
